# Deprecated: schema_registry and debezium_connect are part of the root docker-compose.yml.
# Start the full stack from DataOpsShowcase/: docker compose up -d
# This file is kept empty so legacy commands with -f infra/cdc/docker-compose.cdc.yml do not duplicate services.
services: {}
