# Reference template — optional local overrides when building a custom Atlas image.
# Embedded stack (Docker image sburn/apache-atlas): configuration is bundled in the image;
# ingest scripts use Atlas REST defaults on port 21000.

atlas.authentication.method.file=true
atlas.rest.address=http://localhost:21000
