# Deprecated: atlas_server is part of the root docker-compose.yml.
# Start the full stack from DataOpsShowcase/: docker compose up -d
# This file is kept empty so legacy commands with -f infra/metadata/atlas/docker-compose.atlas.yml do not duplicate services.
services: {}
